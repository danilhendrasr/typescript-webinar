// Print every index in the array
const numbers = [100, 10, 5, 2];
for (const num in numbers) {
    console.log(num);
}

// Print every keys in the object
const objekt = {
    name: "Bobby",
    type: "Cat",
};
for (const key in objekt) {
    console.log(key);
}
