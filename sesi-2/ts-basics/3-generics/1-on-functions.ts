function toArray(x: any, y: any) {
    return [x, y];
}

const result = toArray(1, "Testing");
console.log(result[0].toFixed(2));

// TODO: Change 1 in line 3 to false
// TODO: Convert toArray function to use generics
