/**
 * With JS, it's risky to rename a key in an object.
 */

const objekt = {
    // TODO: Rename this key to "nama"
    name: "Bob",
};

function printName(obj) {
    console.log(obj.name);
}
printName(objekt);
