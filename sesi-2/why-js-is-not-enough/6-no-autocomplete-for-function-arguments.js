/**
 * You don't get autocomplete when doing operations on a function's arguments.
 */

const variable = ["Reverse", "me"];

function reverseArray(array) {
    // TODO: Return reversed version of str
}

const result = reverseArray(variable);
console.log(result);
